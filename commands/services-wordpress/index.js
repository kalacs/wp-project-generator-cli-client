import React from 'react';
import PropTypes from 'prop-types';
import {Text} from 'ink';

/// Wordpress commands
const WordpressIndex = () => <Text>Hello, {name}</Text>;

export default WordpressIndex;
